

const JPH = 'https://jsonplaceholder.typicode.com'
const MAIN = '/api/v1'
export const RestConfig = {
  users: {
    main: `${JPH}/users`
  },
  posts: {
    main: `${JPH}/posts`
  },
  auth: {
    login: `${MAIN}/login`
  }
}